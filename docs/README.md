# llmenv Documentation

The user-facing documentation has moved to the docs site:

**<https://phaedrus1992.github.io/llmenv/>**

Source lives in [`website/docs/`](../website/docs/). Edit there.

The `design/`, `reference/`, and `superpowers/` subdirectories here are internal
development artifacts — design docs, specs, and plans that don't belong on the
public site.
