mcp:
  - name: ctx
    command: ctx-mcp
  - name: playwright
    command: npx
    args: ["-y", "@playwright/mcp@latest"]
    when: ["feature-playwright"]
permissions:
  allow:
    - { tool: Bash, pattern: "cargo *" }
