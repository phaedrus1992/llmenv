{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":configMigration",
    ":semanticCommits",
  ],

  // General settings
  "dependencyDashboard": false,
  "assigneesFromCodeOwners": true,
  // Track main plus every maintained release line (release/1.x, release/2.x, …).
  // Anchored so it excludes the transient forward-merge/release/* branches.
  "baseBranchPatterns": ["main", "/^release\\/.+$/"],
  "branchConcurrentLimit": 5,
  "internalChecksFilter": "strict",
  "labels": ["dependencies"],
  "minimumReleaseAge": "3 days",

  // Open PRs immediately. "not-pending" would defer PR creation until branch
  // checks finish, but CI here triggers on pull_request (not branch push), so a
  // branchless PR never gets checks — "not-pending" then stalls every PR until
  // the ~25h prNotPendingHours fallback. Immediate lets the PR open and trigger
  // its own pull_request CI.
  "prCreation": "immediate",
  "prHourlyLimit": 0,
  "rangeStrategy": "bump",
  "rebaseWhen": "behind-base-branch",
  "semanticCommitType": "chore",
  "semanticCommitScope": "deps",

  // Rust crate updates
  postUpdateOptions: [
    'cargoLockPackages',
  ],

  // The pre-commit manager is disabled by default; .pre-commit-config.yaml
  // exists in this repo, so opt in to keep hook versions updated too.
  "pre-commit": {
    "enabled": true,
  },

  // Package- or manager-specific rules.
  // Rules apply top-to-bottom; later rules override earlier ones for the same
  // field. So: (1) set the automerge/major policy broadly, (2) give each
  // ecosystem its own grouped PR, (3) let the finer sub-groups below carve
  // specific package families out of their ecosystem group.
  "packageRules": [
    {
      // release/1.x and release/2.x are maintenance lines: no routine dependency
      // churn, only security fixes. Disabling normal updates here still leaves
      // vulnerabilityAlerts (a separate remediation path) free to open security
      // PRs against these branches. main and release/3.x keep full updates.
      "description": "Maintenance release lines get security fixes only",
      "matchBaseBranches": ["release/1.x", "release/2.x"],
      "enabled": false,
    },
    {
      "description": "Automerge every non-major update once its branch is green",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true,
    },
    {
      "description": "Major updates: one PR each, reviewed and merged by hand",
      "matchUpdateTypes": ["major"],
      "groupName": null,
      "automerge": false,
    },
    {
      "description": "All non-major Rust crate updates in one PR",
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "rust crates",
      "groupSlug": "rust-crates",
    },
    {
      "description": "All non-major npm updates in one PR",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "npm packages",
      "groupSlug": "npm-packages",
    },
    {
      "description": "All non-major pre-commit hook updates in one PR",
      "matchManagers": ["pre-commit"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "pre-commit hooks",
      "groupSlug": "pre-commit",
    },
    {
      "description": "Group non-major GitHub Actions updates",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "non-major GitHub Actions",
      "groupSlug": "github-actions-non-major",
    },
    {
      "description": "Group Docusaurus package updates together (co-versioned; must move as a unit)",
      "groupName": "docusaurus",
      "groupSlug": "docusaurus",
      "matchManagers": ["npm"],
      "matchPackageNames": ["/^@docusaurus\//", "@easyops-cn/docusaurus-search-local"],
    },
    {
      "description": "Group React package updates together",
      "groupName": "react",
      "groupSlug": "react",
      "matchManagers": ["npm"],
      "matchPackageNames": ["/^react/", "/^@mdx-js\//", "prism-react-renderer"],
    },
    {
      "groupName": "group Rust Serde package updates together",
      "groupSlug": "rust-serde",
      "matchManagers": ["cargo"],
      "matchPackageNames": ["/^serde/"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
    },
    {
      "groupName": "group Rust Tokio package updates together",
      "groupSlug": "rust-tokio",
      "matchManagers": ["cargo"],
      "matchPackageNames": ["/^tokio/"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
    },
  ],

  // CVE fixes: vulnerabilityAlerts defaults already give us separate PRs
  // (groupName: null), no release-age delay (minimumReleaseAge: null), and
  // immediate creation (prCreation: "immediate"). Security PRs also bypass
  // branchConcurrentLimit/prConcurrentLimit, so prPriority would be moot.
  // All we need is the shared "security" label.
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["dependencies", "security"],
  },
}
