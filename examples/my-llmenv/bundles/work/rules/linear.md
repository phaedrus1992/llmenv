---
scope: linear
priority: high
---

# Linear

- Assigning a Linear issue to me: always pass the self-shortcut `"me"` (the MCP `save_issue` `assignee` field and `get_user` query both accept `"me"`). Never resolve me by name or email — a disabled account named "Disabled User" is tied to my email and gets matched instead of my real account.
